name: ArcaneTasks CI

# Aciona o pipeline em pushes ou PRs na branch main
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    # 1. Baixa o código do repositório
    - uses: actions/checkout@v4

    # --- BACKEND ---
    - name: Setup Node.js (Backend)
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        # Cache desativado temporariamente para evitar erros se o package-lock.json não estiver no repo
        # cache: 'npm'
        # cache-dependency-path: backend/package-lock.json

    - name: Install Backend Dependencies
      run: |
        cd backend
        # Usa 'npm install' que é mais tolerante à falta de lockfile que 'npm ci'
        npm install

    # (Descomente abaixo se tiver testes configurados no backend)
    # - name: Run Backend Tests
    #   run: |
    #     cd backend
    #     npm test

    # --- FRONTEND ---
    - name: Setup Node.js (Frontend)
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        # Cache desativado temporariamente
        # cache: 'npm'
        # cache-dependency-path: frontend/package-lock.json

    - name: Install Frontend Dependencies
      run: |
        cd frontend
        npm install

    - name: Lint Frontend
      run: |
        cd frontend
        npm run lint

    - name: Build Frontend
      run: |
        cd frontend
        npm run build